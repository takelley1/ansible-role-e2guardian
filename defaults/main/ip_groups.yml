---

# This list matches host IPs to filter groups.
e2g_ipgroups:
  # filter1 doesn't have to be defined since it's always the default.
  - 192.0.2.1 = filter2
  - 192.0.2.2 = filter3
  - 192.0.2.3 = filter4
